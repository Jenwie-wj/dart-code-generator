name: dart_code_generator
description: A Dart code generator that converts API files to helper files with type transformations
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^6.0.0
  path: ^1.8.0
  args: ^2.4.0

dev_dependencies:
  lints: ^3.0.0
